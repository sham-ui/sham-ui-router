<a
    href="{{this.generateURL( path, params )}}"
    class="{{
        [
            className,
            useActiveClass && this.isActive( path, params ) ? activeClass : ''
        ].join( ' ' ).trim()
    }}"
>
    {{text}}
</a>